---
name: "HaVacation"
description: >-
  Makes your home look occupied while you're away by replaying historical
  Home Assistant entity activity with configurable random jitter.
version: "26.08.1"
slug: "havacation"
url: "https://github.com/davidnajar/HaVacation"
image: "ghcr.io/davidnajar/havacation"

arch:
  - amd64
  - aarch64

init: false
startup: application
boot: auto

homeassistant_api: true

# MQTT is optional. When a broker service exists, HaVacation publishes a
# controllable Vacation Mode switch and a Next Event sensor via MQTT Discovery.
services:
  - mqtt:want

ingress: true
ingress_port: 8080
ingress_stream: true
panel_icon: "mdi:beach"

ports:
  8080/tcp: null
ports_description:
  8080/tcp: "HaVacation web UI (direct access, optional)"

# Runtime settings are managed by the HaVacation UI and persisted under /data.
options: {}
schema: {}

homeassistant: "2025.1.0"
