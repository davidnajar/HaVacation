---
name: "HaVacation"
description: >-
  Makes your home look occupied while you're away by replaying historical
  Home Assistant entity activity with configurable random jitter.
version: "26.08.1"
slug: "havacation"
url: "https://github.com/davidnajar/HaVacation"
image: "ghcr.io/davidnajar/havacation"

arch:
  - amd64
  - aarch64

init: false
startup: application
boot: auto

homeassistant_api: true

# MQTT is optional. If a broker service exists, MQTT Discovery adds a
# controllable Vacation Mode switch. Core replay works without MQTT.
services:
  - mqtt:want

ingress: true
ingress_port: 8080
ingress_stream: true
panel_icon: "mdi:beach"

ports:
  8080/tcp: null
ports_description:
  8080/tcp: "HaVacation web UI (direct access, optional)"

options: {}
schema: {}

homeassistant: "2025.1.0"
